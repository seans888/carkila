<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.prototype.carkila">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".Initial">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Dashboard" />
        <activity android:name=".RenterAuth.RenterRegistration" />
        <activity android:name=".Owner.OwnerHome" />
        <activity android:name=".Owner.LicenseDetails" />
        <activity android:name=".Owner.AddVehicle" />
        <activity android:name=".Owner.VehicleListing" />
        <activity android:name=".Vehicle.DisplayVehicle" />
        <activity android:name=".Renter.TripDetails" />
        <activity android:name=".Vehicle.Vehicle_details" />
        <activity android:name=".OwnerAuth.OwnerRegistration" />
        <activity android:name=".RenterAuth.RenterLogin" />
        <activity android:name=".OwnerAuth.OwnerLogin" />
        <activity android:name=".Renter.RenterHome"></activity>

        <service
            android:name=".MyFirebaseInstanceIDService">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT"/>
            </intent-filter>
        </service>

        <service
            android:name=".MyFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>


    </application>

</manifest>